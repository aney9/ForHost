from .views import *from rest_framework import routersfrom django.urls import pathurlpatterns = []router = routers.SimpleRouter()router.register('brand', BrandViewSet, basename='brand')router.register('pettype', PetTypeSet, basename='pettype')router.register('categoryproduct', CategoryProductSet, basename='categoryproduct')router.register('catalogproduct', CatalogProductSet, basename='catalogproduct')router.register('promotion', PromotionSet, basename='promotion')router.register('order', OrderSet, basename='order')router.register('pos-order', Pos_orderSet, basename='pos-order')urlpatterns += router.urls